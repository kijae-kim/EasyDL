# 🚀 EasyDL 프로젝트 시작하기

> 설정부터 첫 학습까지 완벽 가이드

---

## ✅ 프로젝트 구조

```
EasyDL/
├── 00_학습_가이드/              ← 지금 보고 있는 곳
│   ├── 시작하기.md             ← 이 파일
│   ├── 커리큘럼.md
│   ├── 학습_진행_상황.md
│   └── Obsidian_설정_가이드.md
│
├── 01_Chapter1_딥러닝_시작/     ← Chapter별 학습 자료
│   ├── notes/                  ← Obsidian 학습 노트
│   ├── code/                   ← 실습 코드
│   └── assets/                 ← 이미지, 그래프
│
├── templates/                   ← Obsidian 템플릿
├── scripts/                     ← 자동화 스크립트
│   ├── claude_logger.py        ← Claude 대화 로깅
│   └── tistory_poster.py       ← 티스토리 자동 포스팅
│
├── README.md                    ← 프로젝트 개요
├── requirements.txt             ← Python 패키지
└── .gitignore                   ← Git 제외 파일
```

---

## 📋 Step 1: 환경 설정

### 1-1. Python 가상 환경 생성

```bash
# EasyDL 폴더로 이동
cd /Users/gimgijae/Desktop/Paper/EasyDL

# 가상 환경 생성
python3 -m venv venv

# 가상 환경 활성화
source venv/bin/activate  # macOS/Linux
# 또는
venv\Scripts\activate     # Windows

# 패키지 설치
pip install -r requirements.txt
```

### 1-2. TensorFlow 설치 확인

```bash
python -c "import tensorflow as tf; print(f'TensorFlow {tf.__version__} 설치 완료')"
```

---

## 📘 Step 2: Obsidian 설정

자세한 내용은 [[Obsidian_설정_가이드]]를 참고하세요.

### 빠른 설정

1. **Obsidian 설치**
   ```bash
   brew install --cask obsidian
   ```

2. **Vault 연결**
   - Obsidian 실행
   - "Open folder as vault" 선택
   - `/Users/gimgijae/Desktop/Paper/EasyDL` 선택

3. **필수 플러그인 설치**
   - Templater
   - Dataview
   - Git (선택)

---

## 📝 Step 3: 첫 번째 학습 노트 작성

### 3-1. 새 노트 생성

1. Obsidian에서 `01_Chapter1_딥러닝_시작/notes/` 폴더로 이동
2. 새 노트 생성 (`Cmd/Ctrl + N`)
3. 파일명: `01_퍼셉트론.md`

### 3-2. 템플릿 삽입

1. `Cmd/Ctrl + P` → "Insert template"
2. "학습_노트_템플릿" 선택
3. 정보 입력:
   ```yaml
   chapter: "Chapter 1"
   topic: "퍼셉트론"
   lecture_url: "https://youtu.be/13wbFxjz0d4"
   date: 2025-01-08
   status: 🔄 학습중
   tags: [딥러닝, EasyDL, 퍼셉트론, Chapter1]
   ```

### 3-3. 강의 시청 및 노트 작성

📹 [퍼셉트론 강의 보기](https://youtu.be/13wbFxjz0d4?si=uchyInhxP4PZbqDO)

1. **학습 목표** 섹션 작성
2. 강의 시청하면서 **이론 설명** 작성
3. 코드 실습 → **코드 구현** 섹션에 추가

---

## 💻 Step 4: 코드 실습

### 4-1. 코드 파일 생성

```bash
# code 폴더로 이동
cd 01_Chapter1_딥러닝_시작/code

# Python 파일 생성
touch perceptron.py
```

### 4-2. 코드 작성

```python
# perceptron.py
import numpy as np

class Perceptron:
    def __init__(self, learning_rate=0.01, epochs=100):
        self.lr = learning_rate
        self.epochs = epochs
        self.weights = None
        self.bias = None

    def fit(self, X, y):
        n_samples, n_features = X.shape
        self.weights = np.zeros(n_features)
        self.bias = 0

        for _ in range(self.epochs):
            for idx, x_i in enumerate(X):
                linear_output = np.dot(x_i, self.weights) + self.bias
                y_predicted = self.activation(linear_output)

                update = self.lr * (y[idx] - y_predicted)
                self.weights += update * x_i
                self.bias += update

    def predict(self, X):
        linear_output = np.dot(X, self.weights) + self.bias
        return self.activation(linear_output)

    def activation(self, x):
        return np.where(x >= 0, 1, 0)

# 테스트
if __name__ == "__main__":
    # AND 게이트
    X = np.array([[0, 0], [0, 1], [1, 0], [1, 1]])
    y = np.array([0, 0, 0, 1])

    p = Perceptron()
    p.fit(X, y)

    print("AND 게이트 예측:")
    print(p.predict(X))
```

### 4-3. 코드 실행

```bash
python perceptron.py
```

---

## 🤖 Step 5: Claude와 Q&A

VSCode에서 Claude Code와 대화하며 이해도를 높입니다.

### 질문 예시

```
Q: 퍼셉트론의 활성화 함수는 왜 계단 함수를 사용하나요?

Q: XOR 문제를 퍼셉트론으로 풀 수 없는 이유는 뭔가요?

Q: 가중치 업데이트 공식을 수식으로 설명해주세요.
```

---

## 📊 Step 6: 학습 노트 완성

### 6-1. Claude Q&A 추가

Obsidian 노트의 "🤔 Claude와의 Q&A" 섹션에 질문/답변 추가

### 6-2. 핵심 정리 작성

```markdown
## 💡 핵심 정리

1. **퍼셉트론**: 가장 단순한 형태의 인공 신경망
2. **선형 분류기**: 선형적으로 분리 가능한 문제만 해결 가능
3. **학습 규칙**: Δw = η(y - ŷ)x
4. **한계**: XOR 문제 해결 불가 → MLP 필요
```

### 6-3. 상태 업데이트

```yaml
status: ✅ 완료
```

---

## 🔄 Step 7: Git 커밋

```bash
# 변경사항 확인
git status

# 추가
git add .

# 커밋
git commit -m "Chapter 1: 퍼셉트론 학습 완료

- 퍼셉트론 이론 학습 및 노트 작성
- Python 코드 구현 (AND 게이트)
- Claude와 Q&A 진행

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## 📤 Step 8: 티스토리 포스팅 (선택)

### 8-1. 티스토리 API 설정

`scripts/tistory_config.json` 파일 생성:

```json
{
  "access_token": "YOUR_TISTORY_ACCESS_TOKEN",
  "blog_name": "your-blog-name",
  "category_mapping": {
    "Chapter 1": "123456"
  }
}
```

### 8-2. 자동 포스팅

```bash
# dry_run 모드로 미리보기
python scripts/tistory_poster.py --file "01_Chapter1_딥러닝_시작/notes/01_퍼셉트론.md" --dry-run

# 실제 포스팅
python scripts/tistory_poster.py --file "01_Chapter1_딥러닝_시작/notes/01_퍼셉트론.md"
```

---

## 📈 Step 9: 진행 상황 업데이트

[[학습_진행_상황]] 파일 업데이트:

```markdown
### Chapter 1: 딥러닝 시작 (1/3 완료)

| 강의명 | 상태 | 완료일 | 블로그 |
|--------|------|--------|--------|
| 퍼셉트론 | ✅ 완료 | 2025-01-08 | [링크](#) |
| 신경망 기초 | ⏳ 대기 | - | - |
| 텐서플로우 기초 | ⏳ 대기 | - | - |

**진행률**: ███░░░░░░░ 33%
```

---

## 🔁 Step 10: 반복

다음 강의로 이동하여 Step 3부터 반복!

---

## 🎯 학습 팁

### 1. 매일 조금씩
- 하루 1강의 원칙
- 30분 이상 학습
- 코드는 반드시 직접 작성

### 2. 이해 우선
- 단순 암기 X
- 수식의 의미 파악
- Claude와 충분히 대화

### 3. 실습 중심
- 이론 → 코드 → 실험
- 파라미터 변경하며 실험
- 결과 분석 및 기록

### 4. 복습 필수
- 주말마다 한 주 복습
- 이전 Chapter와 연결
- 블로그 포스팅 검토

---

## 📞 문제 해결

### Git 관련

```bash
# 원격 저장소 연결
git remote add origin https://github.com/kijae-kim/EasyDL.git

# 첫 푸시
git push -u origin main
```

### Python 패키지 에러

```bash
# 가상 환경 재생성
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Obsidian 플러그인 오류

Settings → Community plugins → Reload

---

## 🚀 준비 완료!

이제 본격적으로 학습을 시작할 준비가 되었습니다!

**첫 강의**: [[01_Chapter1_딥러닝_시작/notes/01_퍼셉트론|퍼셉트론 학습 시작하기]]

---

**"The journey of a thousand miles begins with a single step."**

화이팅! 🔥
